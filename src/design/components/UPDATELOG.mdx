---
name: 更新日志
path: /Update
order: 98
---

## 0.0.1-alpha.next

## 0.0.1-alpha.5

feat:

components:

- Form: rules.item 会自动注入 required 的中文提示，根据 control.label
- Form: onSubmitButtonClick 会自动校验表单
- Form: add submitButtonProps props
- Form: add resetButtonProps props
- ModalButton: onConfirm 支持返回 promise 自动管理 loading 和 visible 状态

utils:

- EventBus: listen callback add third param `type`
- remove/insert: pref types 

fix: 

- SourceTable: validateSearchForm 使用了 searchForm 做内部校验
- InputNumber: 去除内部 format hackers
- Table: normalize.ts 修复了未对 undefined 的判断

other: 

- SourceTable: searchForm action 使用 Form 组件自带的
- index.ts 统一导出组件

## 0.0.1-alpha.4

feat: 

- Form: 增加 `actionControlProps` props 用来控制表单按钮的布局
- Table: IColDef 增加 render 函数，详细参考 Table Render 示例


ohter: 

- Table: controlNumberOneRow default value 4 => 1


## 0.0.1-alpha.3

feat: 

- ModalButton: onConfirm 增加默认行为，关闭弹窗
- Loading: add `style` props
- Input: type support `ReactElement`
- Form: add `rowProps`, `colProps` props
- Form: add `layout` props
- Form: add `className` props
- SourceTable: add `header` props
- SourceTable: add `footer` props

fix: 

- SourceTable: 修复 ts 类型拼写错误 `resetable`

other:

- Form: refactor `action` layout from Row to Group
- update `antd` from `^3.13.6` to `^3.14.1`

break-change:

- Form: remove `IFormControl.dataIndex` with `IFormControl.field`
- Form: remove `IFormControl.options` with `IFormControl.decorator`
- Form: remove `labelSpace`,`wrapperSpace`,`compact`,`multiGutter`,`rowClassName`,`onEndAsyncComputed`,`onStartAsyncComputed`
- Form: replace `hideReset` with `resetable`
- Form: replace `save` with `submit`
- Form: replace `onSubmit` with `onSubmitButtonClick`
- Form: replace `onReset` with `onResetButtonClick`


## 0.0.1-alpha.2

feat:

- Input: 接口增加 `after`, `before` 
- SourceList: 实现全新 API，适用更加广泛的业务场景
- Table: 接口支持联动增加 `rules`, `exsitable`

break-change:

- Table: 修改接口 `actions` -> `action`
- Table: 删除创建相关所有接口
- Table: 联动接口类型修改

other

- Table: 增加 rendering class demo
- Table: 重构 Rendering